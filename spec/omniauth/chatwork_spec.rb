RSpec.describe Omniauth::Chatwork do
  it "has a version number" do
    expect(Omniauth::Chatwork::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
